[package]
name = "render-shared"
version = "0.1.0"
edition = "2021"

[dependencies]
bytemuck = { version = "1", features = ["derive"] }
glam = "0.32"
serde = { version = "1", features = ["derive"] }
pt-mats = { path = "../pt-mats" }

[dev-dependencies]
serde_json = "1"
